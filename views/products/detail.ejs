<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= product.name %> | Product Details</title>

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">

  <div class="max-w-6xl mx-auto px-4 py-10">

    <!--Back Button -->
    <a
      href="/"
      class="inline-block mb-6 text-blue-600 hover:underline"
    >
      ← Back to products
    </a>

    <!--PRODUCT CARD -->
    <div class="bg-white rounded-lg shadow-lg overflow-hidden grid grid-cols-1 md:grid-cols-2">

      <!--IMAGE -->
      <div class="bg-gray-50 p-6 flex items-center justify-center">
        <img
          src="/uploads/products/<%= product.image %>"
          alt="<%= product.name %>"
          class="max-h-[400px] w-full object-contain rounded"
        />
      </div>

      <!--DETAILS -->
      <div class="p-8 flex flex-col justify-between">

        <div>
          <!-- Category -->
          <span class="inline-block mb-3 text-sm bg-gray-200 text-gray-700 px-3 py-1 rounded-full">
            <%= product.category.name %>
          </span>

          <!--Product Name -->
          <h1 class="text-3xl font-bold text-gray-800 mb-4">
            <%= product.name %>
          </h1>

          <!-- Description -->
          <p class="text-gray-600 leading-relaxed">
            <%= product.description %>
          </p>
        </div>

        <!-- Price (optional but recommended) -->
        <% if (product.price) { %>
          <div class="mt-6">
            <span class="text-2xl font-semibold text-blue-600">
              ₹ <%= product.price %>
            </span>
          </div>
        <% } %>

      </div>
    </div>

  </div>

</body>
</html>
